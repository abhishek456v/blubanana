# Environment variables — never commit real secrets
.env
.env.local
.env.*.local

# Expo / React Native
node_modules/
.expo/
dist/
# …except the website's output, which is what gets deployed. It is built here
# rather than on the host because the build reads the Supabase URL and anon key
# out of .env to wire up the live price, and .env is not on the host.
!website/dist/
website/.preview/
build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Supabase CLI local state
supabase/.temp/

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
# Screenshots from platform/scripts/drive.mjs. Regenerate, do not commit.
screenshots/

# Upload packages, rebuilt by `npm run package`
deploy/
